{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/$1" }
  ],
  "buildCommand": "cd .. && turbo run build --filter=web --filter=storybook",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
